// Public-safe firm contact details shown on every property (never the owner's private
// phone/email — those live only in the internal `properties` table, never in
// `public_properties`).
export const FIRM_PHONE = "+919820000000";
export const FIRM_WHATSAPP = "+919820000000";
